<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lfm.wms.dao.UserInfoMapper">
    <resultMap id="BaseResultMap" type="com.lfm.wms.model.UserInfo">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="salt" jdbcType="VARCHAR" property="salt"/>
    </resultMap>

    <sql id="Base_Column_List">
      id,user_name,password,salt
    </sql>

    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_info
        where id = #{id,jdbcType=VARCHAR}
    </select>
    <select id="selectByName" parameterType="string" resultMap="BaseResultMap">
      select
      <include refid="Base_Column_List"/>
        from user_info
        where user_name=#{username}
    </select>
    <select id="getAllUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_info
    </select>
    <update id="updateUser" parameterType="com.lfm.wms.model.UserInfo">
        update user_info
        set user_name=#{userName}
        where id =
        #{id}
    </update>
    <delete id="deleteUser" parameterType="Integer">
        delete from  user_info
        where
        id=#{id}
    </delete>
    <select id="selectAll" resultMap="BaseResultMap" parameterType="com.lfm.wms.model.UserInfo">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select *
        from user_info
        where 1=1
        <if test="userName !=null and userName !=''">
            and user_name like concat('%',#{userName},'%')
        </if>
    </select>
    <insert id="addUser" parameterType="com.lfm.wms.model.UserInfo">
        insert into user_info (user_name,password,salt) values (#{userName},#{password},#{salt})
    </insert>

</mapper>


